language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sstathatos/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sstathatos/docker-react npm run test -- --coverage